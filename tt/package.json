{
  "name": "tt-cli",
  "version": "0.1.0",
  "description": "Fronts your test runner and manages what reaches agent context — verdict by default, full detail on demand, all from one cached run",
  "type": "module",
  "bin": {
    "tt": "bin/tt.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@toon-format/toon": "^2.3.0"
  }
}
