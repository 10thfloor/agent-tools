{
  "name": "tj-cli",
  "version": "0.1.0",
  "description": "TOON-ify any JSON-speaking CLI for coding agents: per-CLI prune profiles, gh/kubectl/aws aware",
  "type": "module",
  "bin": {
    "tj": "bin/tj.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@toon-format/toon": "^2.3.0"
  }
}
