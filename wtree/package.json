{
  "name": "wtree-cli",
  "version": "0.1.0",
  "description": "Developer-friendly git worktree manager: one-command create/remove, activity-aware listing, TOON output for agents",
  "type": "module",
  "bin": {
    "wtree": "bin/wtree.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@toon-format/toon": "^2.3.0"
  }
}
