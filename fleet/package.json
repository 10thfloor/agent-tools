{
  "name": "fleet-cli",
  "version": "0.1.0",
  "description": "Cross-project overview of repos, worktrees, PRs, and live agents — one table for the whole fleet",
  "type": "module",
  "bin": {
    "fleet": "bin/fleet.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@toon-format/toon": "^2.3.0"
  }
}
